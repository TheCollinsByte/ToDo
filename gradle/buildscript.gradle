// Build-time repositories only.
repositories {
    mavenCentral()
    maven {
        url "https://plugins.gradle.org/m2/" // for error prone plugin
    }
}

// Build-time dependencies only.
dependencies {
    classpath "com.github.node-gradle:gradle-node-plugin:5.0.0"  // for NPM, Yarn
}